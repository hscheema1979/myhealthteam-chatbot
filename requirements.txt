# MyHealthTeam Chatbot Requirements
# Python 3.10+

# Web Framework
streamlit>=1.28.0

# Database
# (sqlite3 is built into Python standard library)

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Note: Gemini CLI is installed via npm, not pip
# Install with: npm install -g @google/gemini-cli
